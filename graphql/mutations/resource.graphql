mutation DeclareResource(
  $backend: ID!
  $name: String!
  $localId: String!
  $qualifiers: [QualifierInput!]
) {
  declareResource(
    input: {
      backend: $backend
      name: $name
      localId: $localId
      qualifiers: $qualifiers
    }
  ) {
    ...Resource
  }
}
