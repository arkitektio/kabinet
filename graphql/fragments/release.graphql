fragment Release on Release {
  id
  version
  app {
    identifier
  }
  scopes
  flavours {
    ...ListFlavour
  }
}

fragment ListRelease on Release {
  id
  version
  app {
    identifier
  }
  scopes
  flavours {
    ...ListFlavour
  }
}
